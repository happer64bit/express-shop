<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Shop Eco</title>
    <link rel="stylesheet" href="/global.css">
    <style>
        .container {
            display: grid;
			grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
			grid-gap: 20px;
			padding: 20px;
        }
    </style>
</head>
<body>
    <nav class="navbar">
        <h1>Shop Eco</h1>
        <div style="min-width: 12rem; max-width: 20rem;">
            <form action="/search" method="get">
                <input type="text" placeholder="Search Anything" style="padding: 12px; border: none; background-color: #ddd; border-radius: 5px; width: 100%; outline: none;" id="search-input" name="q">
            </form>
        </div>
        <div style="padding: 1px;">
            <div style="display: flex; align-items: center; padding-right: 10px;">
                <% if(!isLoggedIn) {%>
                    <a href="/account/login" class="teal-btn" aria-label="Login">Login</a>
                    <div style="padding: 4px;"></div>
                    <a href="/account/register" class="teal-btn" aria-label="Register">Register</a>
                <% } else {%>
                    <% if(user.accountType != "personal") { %>
                        <a href="/pannel" class="teal-btn" aria-label="Profile">Pannel</a>
                        <div style="padding: 4px;"></div>
                    <% } else { %>

                    <% } %>
                    <a href="/account/profile" class="teal-btn" aria-label="Profile">Profile </a>
                    <div style="padding: 4px;"></div>
                    <a href="/account/logout" class="teal-btn" aria-label="Logout">Logout</a>
                <% } %>
            </div>
        </div>
    </nav>
    <main style="padding-top: 6pc;">
        <div class="container">
            <% for(var i = 0; i < items.length; i++) { %>
                <div style="padding: 1pc;">
                    <img src="/images/<%= items[i].image %>" width="150px"/>
                    <h3><%= items[i].name %></h3>
                    <a href="/checkout/<%= items[i]._id %>">Checkout</a>
                </div>
            <% }%>
        </div>
    </main>
</body>
</html>
