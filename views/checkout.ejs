<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Checkout <%= item.name %>
    </title>
    <link rel="stylesheet" href="/global.css">
</head>

<body>
    <nav class="navbar">
        <a href="/">
            <h1>Shop Eco</h1>
        </a>
        <div style="min-width: 12rem; max-width: 20rem;">
            <form action="/search" method="get">
                <input type="text" placeholder="Search Anything"
                    style="padding: 12px; border: none; background-color: #ddd; border-radius: 5px; width: 100%; outline: none;"
                    id="search-input" name="q">
            </form>
        </div>
        <div style="padding: 1px;">
            <div style="display: flex; align-items: center; padding-right: 10px;">
                <% if(user.accountType !="personal" ) { %>
                    <a href="/pannel" class="teal-btn" aria-label="Profile">Pannel</a>
                    <div style="padding: 4px;"></div>
                <% } else { %>

                <% } %>
                <a href="/account/profile" class="teal-btn" aria-label="Profile">Profile </a>
                <div style="padding: 4px;"></div>
                <a href="/account/logout" class="teal-btn" aria-label="Logout">Logout</a>
            </div>
        </div>
    </nav>
    <div style="padding-top: 6pc; display: flex; justify-content: center; height: 100%;">
        <div style="width: 400px;">
            <div style="text-align: center;">
                <img src="/images/<%= item.image %>" height="250px"/>
            </div>
            <div>
                <h3><%= item.name %></h3>
                <h5>
                    by <b>
                        <a href="/profile/<%= owner._id %>"><%= owner.username %></a>
                    </b>
                </h5>
                <p><%= item.description %></p>
            </div>
            <div>
                <code>Supply: <%= item.supply %></code>
            </div>
            <div>
                <a href="/comfirmPayment?saleId=<%= item._id %>" style="background-color: rgb(60, 60, 255); color: white; padding: 12px 16px; width: 400px; border-radius: 5px;">Buy For <%= item.price %> USD</a>
            </div>
        </div>
    </div>
</body>

</html>